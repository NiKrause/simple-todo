// Disable prerendering for this page since it uses form actions
export const prerender = false;

// Page configuration for hybrid mode
export const ssr = true; // Enable SSR when server is available
